name: example-chrome
on: [push]
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: install
        env:
          email: ${{ secrets.email }}
        run: |
          npm install
          npm run cy:ci
        working-directory: ./

